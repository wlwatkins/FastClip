// Registered via vitest.config.ts `test.setupFiles`. Extends `expect` with
// jest-dom's DOM matchers (toBeInTheDocument, toHaveTextContent, ...). Without
// this import the matchers are installed in node_modules but inert.
import "@testing-library/jest-dom/vitest";
