#!/usr/bin/env bash
# See .githooks/pre-commit. Same policy, applied to push.
if [ "$ALLOW_PUSH" != "1" ]; then
  echo "" >&2
  echo "  Push blocked. No AI pushes on this project." >&2
  echo "  If you are the owner:  ALLOW_PUSH=1 git push" >&2
  echo "" >&2
  exit 1
fi
exit 0
